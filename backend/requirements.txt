# --- Web ---
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.12
httpx>=0.27.2
pydantic>=2.9.2
pydantic-settings>=2.5.2

# --- Извлечение текста из документов ---
pypdf>=5.1.0            # .pdf
python-docx>=1.1.2      # .docx
python-pptx>=1.0.2      # .pptx
openpyxl>=3.1.5         # .xlsx
beautifulsoup4>=4.12.3  # .html/.htm, парсинг результатов поиска
striprtf>=0.0.28        # .rtf
# .odt/.ods/.odp/.epub/.zip-контейнеры читаются через стандартный zipfile + BeautifulSoup

# --- Опционально (раскомментируйте при необходимости) ---
# pymupdf>=1.24.0       # быстрый и качественный парсинг PDF (AGPL)
# pytesseract>=0.3.13   # OCR картинок (требует установленный tesseract + языки chi_sim/eng)
# Pillow>=10.4.0
